Module modTaskInfo
    Structure TaskInfo
        Dim strName As String
        Dim dtDate As Date
        Dim dtTime As Date
        Dim blnComplete As Boolean
        Dim strCategory As String
    End Structure
End Module
